<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:id="@+id/activity_title"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="3dp"
                android:paddingRight="3dp"
    android:background="@android:color/white"
                tools:context="com.abstractx1.mydiary.TitleActivity">

    <TextView xmlns:android="http://schemas.android.com/apk/res/android"
              android:id="@+id/titeTextView"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_margin="5dp"
              android:padding="10dp"
              android:textSize="12sp"
              android:text="These questions are designed to help you reflect on your perceptions, emotions, and actions that are evoked from using your technology (E.G: health/fitness/wellbeing application; self-tracking device; wearable). Please be as detailed as possible when answering each question. Upon finishing submit your answers via the send button in the toolbar:"/>

    <ListView
        android:id="@+id/questionsListView"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:paddingLeft="3dp"
        android:paddingRight="3dp"
        android:background="@android:color/white"
        android:cacheColorHint="@android:color/transparent"
        android:choiceMode="singleChoice"
        android:fadeScrollbars="false"
        android:requiresFadingEdge="vertical"
        android:descendantFocusability="beforeDescendants"
        android:layout_below="@+id/titeTextView"/>
</RelativeLayout>
